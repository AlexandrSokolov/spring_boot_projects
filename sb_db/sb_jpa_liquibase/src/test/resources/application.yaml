spring:
  jpa:
    show-sql: true

logging:
  level:
    com:
      zaxxer:
        # HikariConfig - all database settings, including the db connection string
        hikari: off
    liquibase: off
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: debug
      springframework: info
      jboss:
        resteasy: info