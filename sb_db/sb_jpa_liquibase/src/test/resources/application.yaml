spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:hsqldb:mem:myDb
    username: sa
    password: sa
    driverClassName: org.hsqldb.jdbc.JDBCDriver

logging:
  level:
    org:
      springframework: info
      jboss:
        resteasy: info